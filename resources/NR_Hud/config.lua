Config              = {}

-- Variables (HUD)
Config.maxPlayers   = 256 -- Keep same as sv_maxclients within your server.cfg.
Config.hideArmor    = true


Config.waitTime     = 400  -- Set to 100 so the hud is more fluid. However, performance will be affected.
Config.waitSpawn    = 4000 -- Time to set elements back to saved on spawn
Config.waitResource = 1000 -- Time to set elements back to saved after resource start

-- Config.voiceKey     = 'Z' -- Must be in caps
Config.openKey      = 'f7' -- Key for opening the customizing menu
Config.oxygenMax    = 10 -- Set to 10 / 4 if using vMenu
